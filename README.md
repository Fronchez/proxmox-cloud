# Proxmox Cloud

Панель управления для Proxmox VE с веб-интерфейсом и Telegram-ботом.

## Возможности

- 🖥️ Создание и управление VM (QEMU)
- 📦 Создание и управление LXC контейнерами
- 🔐 JWT аутентификация
- 🤖 Telegram-бот для администрирования
- 📊 Веб-интерфейс с реальным временем
- ☁️ Cloud-Init для автоматической настройки
- 🔑 Автогенерация паролей

## Быстрый старт (Docker)

```bash
# Клонировать репозиторий
git clone https://github.com/Fronchez/proxmox-cloud.git
cd proxmox-cloud

# Создать файл окружения
cp .env.example .env
nano .env  # Отредактируйте значения

# Запустить все сервисы
docker compose up -d --build
```

## Доступ

| Сервис | URL | Описание |
|--------|-----|----------|
| Web UI | http://localhost:3000 | Веб-интерфейс |
| API Docs | http://localhost:8000/docs | Swagger документация |
| Health Check | http://localhost:8000/health | Проверка статуса |

## Настройка .env

```env
# Proxmox настройки
PROXMOX_HOST=192.168.1.10          # IP адрес Proxmox сервера
PROXMOX_NODE=pve                    # Имя ноды
PROXMOX_TOKEN_ID=root@pam!bot       # ID токена API
PROXMOX_TOKEN_SECRET=xxx            # Секрет токена

# База данных (не меняйте для Docker)
DATABASE_URL=postgresql+asyncpg://proxmox:proxmox@db/proxmox

# Безопасность
SECRET_KEY=your-secret-key-change-in-production

# Telegram бот
TELEGRAM_TOKEN=BOT_TOKEN            # Токен от @BotFather
ADMIN_TELEGRAM_ID=123456789         # Ваш Telegram ID
```

## Создание токена Proxmox

1. Зайдите в Proxmox VE Web UI
2. Перейдите в **Datacenter → Permissions → API Tokens**
3. Нажмите **Add**
4. Выберите пользователя (например, `root@pam`)
5. Введите имя токена (например, `bot`)
6. Скопируйте секрет и укажите в `.env`

## Telegram-бот команды

| Команда | Описание |
|---------|----------|
| /start | Главное меню |
| /create | Создать VM |
| /list | Показать список VM |

## Структура проекта

```
proxmox-cloud/
├── backend/
│   ├── app/
│   │   ├── main.py          # Точка входа FastAPI
│   │   ├── auth.py          # JWT аутентификация
│   │   ├── bot.py           # Telegram бот
│   │   ├── config.py        # Настройки
│   │   ├── database.py      # Подключение к БД
│   │   ├── models.py        # SQLAlchemy модели
│   │   ├── schemas.py       # Pydantic схемы
│   │   ├── proxmox.py       # Proxmox API клиент
│   │   └── routers/
│   │       ├── auth.py      # Роуты аутентификации
│   │       ├── vms.py       # Роуты VM
│   │       └── lxc.py       # Роуты LXC
│   ├── Dockerfile
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── App.jsx
│   │   ├── api.js
│   │   └── pages/
│   │       ├── Login.jsx
│   │       ├── Register.jsx
│   │       └── Dashboard.jsx
│   ├── Dockerfile
│   ├── nginx.conf
│   ├── package.json
│   └── vite.config.js
├── docker-compose.yml
├── .env.example
└── README.md
```

## Разработка

### Backend

```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload
```

### Frontend

```bash
cd frontend
npm install
npm run dev
```

## Безопасность

⚠️ **Важно:**
- Смените `SECRET_KEY` в production
- Используйте HTTPS для продакшена
- Ограничьте CORS в `main.py` для конкретных доменов

## Устранение проблем

### Бот не запускается

1. Проверьте токен бота в @BotFather
2. Убедитесь, что `ADMIN_TELEGRAM_ID` указан верно
3. Проверьте логи: `docker logs proxmox-bot`

### Ошибка подключения к Proxmox

1. Проверьте доступность сервера Proxmox
2. Убедитесь, что токен действителен
3. Проверьте имя ноды в настройках

### Frontend не подключается к API

1. Укажите правильный `VITE_API_URL` при сборке
2. Проверьте CORS настройки в `backend/app/main.py`

## Лицензия

MIT
